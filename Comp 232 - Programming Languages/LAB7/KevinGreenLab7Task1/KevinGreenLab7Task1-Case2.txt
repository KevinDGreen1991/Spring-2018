the <$> $600.05 <$> if something only then sldo if two else then <!> bank <!> <!> financial <!>
the if something only then sldo if two else then <!> dollar <!>
<$> $20.10 <$>
<#> 405 <#>

POTENTIALLY INTERESTING READING!
Reported sum of numbers: 1025.15
Concordance:
1 dollar
1 financ
1 budget

Process finished with exit code 0