the <$> $1000.05 <$> if something only then sldo if two else then <!> bank <!> <!> financial <!>
the if something only then sldo if two else then <!> dollar <!>
<$> $20.10 <$>
<#> 405 <#>

VERY INTERESTING READING!
Reported sum of dollars: 1020.15

Process finished with exit code 0