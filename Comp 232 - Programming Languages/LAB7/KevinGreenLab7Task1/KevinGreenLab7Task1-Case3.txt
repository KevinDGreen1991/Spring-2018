the <$> $20.05 <$> if something only then sldo if two else then <!> bank <!> <!> financial <!>
the if something only then sldo if two else then <!> dollar <!>
<$> $20.10 <$>
<#> 405 <#>
BORING READING!

Process finished with exit code 0